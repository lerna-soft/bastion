# HIM-004 — Implementar Diseño Stitch "Terminal Core"

**Feature:** Redesign UI con diseño Stitch "SSH Terminal Manager"
**Prioridad:** alta
**Estimación:** 8 puntos
**Estado:** en-progreso
**Dependencias:** ninguno

---

## Historia de usuario

Como usuario de Bastion
Quiero que la interfaz coincida con el diseño Stitch "Terminal Core"
Para tener una experiencia visual profesional, técnica y coherente con el brand.

---

## Alcance

### Incluye
1. **Color.kt** — Reemplazar todos los tokens de color con los valores exactos del diseño Stitch (paleta verde obsidiana)
2. **Sidebar** — Layout correcto: logo Bastion, "New Connection" button, nav items (Connections, Terminal, SSH Keys, Settings), Help/Logs, avatar
3. **TopNavBar** — h:48dp con tabs Sessions/Clusters/History, search input, notificaciones, Connect button
4. **TerminalTab** — Session tabs bar con active indicator (border-t-2), terminal viewport, System Stats panel colapsable
5. **Footer** — h:32dp con copyright, status, links
6. **Command Palette** — Overlay glassmorphism con blur backdrop
7. **System Stats Panel** — Panel derecho colapsable con CPU/RAM/Disk/Network/Logs

### Excluye
- Reemplazar VaultTabContent (se mantiene el actual)
- Pantallas de Configuración y SSH Keys (solo cambiar colores)
- Logo SVG (se mantiene el actual)

---

## Especificación técnica

### 1. Color.kt — Tokens actualizados

Reemplazar TODOS los valores con los del diseño Stitch real:

```kotlin
// Design system "Terminal Core" — dark only (MVP)
val StitchBackground = Color(0xFF0c160a)        // surface / surface-dim
val StitchSurface = Color(0xFF0c160a)
val StitchSurfaceDim = Color(0xFF0c160a)
val StitchSurfaceBright = Color(0xFF313c2e)
val StitchSurfaceContainerLowest = Color(0xFF071106)
val StitchSurfaceContainerLow = Color(0xFF141e12)
val StitchSurfaceContainer = Color(0xFF182216)
val StitchSurfaceContainerHigh = Color(0xFF222d20)
val StitchSurfaceContainerHighest = Color(0xFF2d382a)
val StitchSurfaceVariant = Color(0xFF2d382a)
val StitchOnSurface = Color(0xFFdae6d2)
val StitchOnSurfaceVariant = Color(0xFFb9ccb2)
val StitchPrimary = Color(0xFFebffe2)             // casi blanco
val StitchOnPrimary = Color(0xFF003907)
val StitchPrimaryContainer = Color(0xFF00ff41)    // NEON GREEN
val StitchOnPrimaryContainer = Color(0xFF007117)
val StitchPrimaryFixed = Color(0xFF72ff70)
val StitchPrimaryFixedDim = Color(0xFF00e639)
val StitchOnPrimaryFixed = Color(0xFF002203)
val StitchSecondary = Color(0xFFadc7ff)           // Electric Blue
val StitchOnSecondary = Color(0xFF002e68)
val StitchSecondaryContainer = Color(0xFF4a8eff)
val StitchOnSecondaryContainer = Color(0xFF00285b)
val StitchTertiary = Color(0xFFfff8f4)
val StitchOnTertiary = Color(0xFF442b10)
val StitchTertiaryContainer = Color(0xFFffd5ae)
val StitchOnTertiaryContainer = Color(0xFF7a5b3c)
val StitchOutline = Color(0xFF84967e)
val StitchOutlineVariant = Color(0xFF3b4b37)
val StitchError = Color(0xFFffb4ab)
val StitchOnError = Color(0xFF690005)
val StitchErrorContainer = Color(0xFF93000a)
val StitchOnErrorContainer = Color(0xFFffdad6)
```

### 2. AppLayout.kt — Sidebar y Header

**Sidebar:**
- Ancho: 260dp (colapsado: 64dp con animación existente)
- Logo brand: Ícono/avatar + "Bastion" (headline-md, primary-fixed-dim) + "Infrastructure Management" (ui-label-bold, on-surface-variant opacity-60)
- [New Connection]: Botón primary-container full-width con icono add, text-on-primary-fixed
- Nav items con íconos Material (dns, terminal, vpn_key, settings):
  - Active: border-l-2 (secondary-container) + bg-surface-container-high + text-primary-fixed-dim
  - Inactive: text-on-surface-variant, hover:bg-surface-container-highest
- Spacer weight(1f)
- Help + Logs links
- Avatar section: Circle 36dp + nombre + rol + shield icon

**TopNavBar (AppHeader):**
- h:48dp (actual 64dp → reducir)
- Tabs: Sessions (active: border-b-2 primary-fixed-dim), Clusters, History
- Search: input con icono search, bg-surface-container-lowest, border outline-variant
- Acciones: notificaciones bell, comment icon, [Connect] button secondary-container
- Fondo: bg-surface-container, border-b outline-variant

### 3. TerminalTab.kt — Terminal workspace + System Stats

**Session tabs** (se mantienen en AppLayout.TerminalPagerContent):
- Active tab: bg-surface-container-high + border-t-2 primary-container
- Inactive: text-on-surface-variant, hover:bg-surface-container
- Tab content: icon terminal (16dp) + server name (ui-label-bold) + close

**Terminal viewport:**
- Padding: 16dp (terminal-padding)
- Font: JetBrains Mono 14sp
- Color: primary-fixed-dim
- Background con radial gradient sutil

**SystemStatsPanel** (nuevo, colapsable):
- Ancho: cuando expandido 288dp, colapsado 0dp
- Header: "System Stats" + server name
- CPU bar: label + percentage + progress bar con glow (#00ff41 shadow)
- RAM bar: label + used/total + progress bar
- Disk bar: label + used/total + progress bar (on-tertiary-container)
- Network: Downlink (↓ primary-container) + Uplink (↑ on-secondary-container)
- Connection Logs: timestamps + eventos recientes
- Animación: animateDpAsState para expand/collapse

**Footer** (nuevo):
- h:32dp, bg-surface-container-lowest, border-t outline-variant
- Left: copyright + connected server info
- Right: links + status indicator (●) + UTF-8

**Command Palette** (nuevo):
- Overlay fullscreen con backdrop blur
- Panel glassmorphism centrado
- Input con icon terminal
- Recent commands list

### 4. Estados de componentes

| Componente | Normal | Active/Hover | Disabled |
|-----------|--------|-------------|----------|
| Primary button | bg=primary-container, text=on-primary-fixed | hover:brightness-110, active:scale-95 | — |
| Secondary button | border=secondary-container, text=secondary-fixed-dim, bg=secondary-container/20 | hover:brightness-110 | — |
| Sidebar nav item | text=on-surface-variant | active: border-l-2 + bg-high + text-primary-fixed-dim / hover: bg-highest | — |
| Status dot Online | bg=primary-container + glow shadow + pulse | — | — |
| Status dot Connecting | bg=tertiary-container + pulse | — | — |
| Status dot Offline | bg=outline | — | — |

---

## Gherkin scenarios

```gherkin
Escenario: Sidebar muestra secciones correctas
  Dado que el usuario abre Bastion
  Entonces ve el sidebar con: logo Bastion, botón "New Connection", y nav items Connections, Terminal, SSH Keys, Settings
  Y el nav item activo tiene border izquierdo azul y fondo highlight

Escenario: Terminal tiene session tabs con indicador activo
  Dado que el usuario tiene sesiones SSH abiertas
  Entonces ve las tabs en la parte superior con border-top verde para la activa
  Y puede cerrar tabs con el botón X

Escenario: System Stats panel se puede colapsar
  Dado que el usuario está en una terminal activa
  Cuando toca el botón de stats
  Entonces el panel derecho se expande/colapsa con animación

Escenario: Command Palette se abre con shortcut
  Dado que el usuario está en cualquier pantalla
  Cuando presiona ⌘K (o Ctrl+K)
  Entonces se muestra el overlay glassmorphism con input de búsqueda

Escenario: Footer muestra estado de conexión
  Dado que hay una sesión activa
  Entonces el footer muestra el estado "Connected" con indicador verde
```

---

## Checklist implementación

- [ ] Color.kt: todos los tokens reemplazados
- [ ] AppLayout.kt: Sidebar rediseñada (brand, botón, nav items, active states, avatar)
- [ ] AppLayout.kt: TopNavBar reducida a 48dp con tabs Sessions/Clusters/History
- [ ] AppLayout.kt: Footer agregado (32dp)
- [ ] TerminalTab.kt: SystemStatsPanel colapsable con CPU/RAM/Disk/Network/Logs
- [ ] TerminalTab.kt: Terminal viewport padding correcto
- [ ] TerminalTab.kt: Command Palette con glassmorphism
- [ ] TerminalTab.kt: SpecialKeysBar estilizada con diseño Stitch
- [ ] Verificar build limpio `./gradlew assembleDebug`
